rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Default: tolak semua akses. Sesuaikan aturan di bawah ini
    // sesuai kebutuhan aplikasi QuParkir sebelum production.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
